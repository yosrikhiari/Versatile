import { describe, it, expect } from 'vitest'
import { {{camelCase name}} } from '../../composables/{{camelCase name}}'

describe('{{camelCase name}}', () => {
  it('returns its public API', () => {
    const result = {{camelCase name}}()
    expect(result).toBeTypeOf('object')
  })
})
