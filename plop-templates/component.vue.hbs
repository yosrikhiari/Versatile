<script setup>
/**
 * {{pascalCase name}} — {{description}}
 */
defineProps({
  // TODO: declare props
})

const emit = defineEmits([])
void emit
</script>

<template>
  <div class="{{kebabCase name}}">
    <!-- TODO: {{pascalCase name}} markup -->
  </div>
</template>
