import { defineStore } from 'pinia'
import { ref } from 'vue'

/**
 * {{camelCase name}} store — {{description}}
 */
export const use{{pascalCase name}}Store = defineStore('{{camelCase name}}', () => {
  const items = ref([])

  // TODO: state, getters, actions

  return { items }
})
