name: CI

on:
  push:
    branches: [master, develop, feature/*]
  pull_request:
    branches: [master, develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '22.x'

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: ESLint
        run: npm run lint

      - name: TypeScript typecheck
        run: npm run typecheck

      - name: Design-system and repo policies
        run: npm run lint:tokens && npm run policy

      - name: Prettier formatting check
        run: npx prettier --check "src/**/*.{js,vue,css}"

      - name: Generate ESLint report
        run: npx eslint src/**/*.{js,vue} --format json -o eslint-report.json
        continue-on-error: true

      - name: Upload ESLint report
        uses: actions/upload-artifact@v4
        with:
          name: eslint-report
          path: eslint-report.json
        if: always()

  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    strategy:
      matrix:
        node-version: [22.x]

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      # The lockfile must carry every entry the Linux runner resolves, including
      # optional peers that a Windows `npm install` prunes (@emnapi/*); those are
      # pinned as devDependencies for that reason. If this step fails with
      # "package.json and package-lock.json are not in sync", the lock was
      # regenerated on a machine that dropped them - see CHANGELOG 2026-09-15.
      - name: Install dependencies
        run: npm ci

      # One pass: `vitest run --coverage` is the same suite as `test:run` plus
      # the report, so running both doubled the job for no extra signal.
      - name: Unit tests with coverage
        run: npm run test:coverage

      # Nothing else in this workflow compiles the app in production mode: the
      # e2e job serves it through `npm run dev`. A rollup-only failure (chunking,
      # a dep that resolves differently under build) therefore shipped green.
      - name: Production build
        run: npm run build

      - name: Upload coverage reports
        if: matrix.node-version == env.NODE_VERSION
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/coverage-final.json
          fail_ci_if_error: false

  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: [lint]

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run Playwright E2E tests
        run: npm run test:e2e

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

  backend:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('backend/**/*.csproj', 'backend/**/*.slnx') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore dependencies
        run: dotnet restore backend/Versatile.slnx

      - name: Build
        run: dotnet build backend/Versatile.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test backend/Versatile.slnx --configuration Release --no-build
