# Versatile: Gemini CLI configuration

All agent instructions live in `AGENTS.md` at the repository root (shared with every coding agent). Read it first and follow it; this file exists only so Gemini CLI finds it.
